import Phaser from 'phaser'

export default class Tree extends Phaser.GameObjects.Components {
  // constructor(scene, x, y) {
  //   const triangle = new Phaser.GameObjects.Triangle(scene, 20, 20, 25, 5, )
  // }
}